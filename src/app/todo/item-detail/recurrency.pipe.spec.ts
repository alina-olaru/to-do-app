import { RecurrencyPipe } from './recurrency.pipe';

describe('RecurrencyPipe', () => {
  it('create an instance', () => {
    const pipe = new RecurrencyPipe();
    expect(pipe).toBeTruthy();
  });
});
